# GenAI-Projects
All projects related to GenAI

## Update submodule repos to latest update
```

git submodule update --init --remote --merge

git add .
git commit -m "Update submodules to latest"

git push origin main

```
